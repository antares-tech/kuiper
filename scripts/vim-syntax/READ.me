               VIM SYNTAX FILES FOR 3A SCENARIO FILES (*.case)
---------------------------------------------------------------------------

mkdir -p ~/.vim
cp -rav ftpdetect syntax ~/.vim/

Do make sure that the syntax is on in your .vimrc
